#include <stdio.h>

int main()
{
    int a = 0 , b = 1 , c , i , n;
    printf("define the limit of the fibocanni series");
    scanf("%d" , &n);
    
    c = a + b;
    
    for(i=1, i==n , i++ , a++ , b++);
    {
        printf("%d" ,c);
    }

    return 0;
}
